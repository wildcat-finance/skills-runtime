# Runbook: repair Lazarus macOS path handling

This runbook implements the `descriptor-root-alias` candidate selected by the
checked study. The existing fixture formats, Python pin, dependencies, Linux
workflow, and Lazarus frontier remain unchanged.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 9d7e2d80bd7bd22fd1a980f5e003ca9293e4839b46d667d9f860ce830c2d59ba
candidate | descriptor-root-alias
```

## Step 1: Keep Goldfinch fixture work descriptor-rooted

**Goal.** Add an explicit descriptor-root confinement surface and use it for Goldfinch manifest construction, verification, and stage publication without recovering a pathname.
**Entry.** Run branch `fiat/881-macos-path-repair-clean-run` at starting commit `0987aa37f5110501b2c7a440f42370f81d58afe5`, with the design lock above accepted at `step:1` and no product changes.
**Exit.** Descriptor-root helpers duplicate caller-owned directory descriptors, preserve existing path-root callers, and keep component reads, manifest writes, verification, finalisation, rollback, and cleanup anchored to the pinned stage. The Goldfinch builder executes on Darwin without the pseudo-FD skip and reproduces `plugins/lazarus/examples/goldfinch-v1` byte for byte. Prove the exit with `TMPDIR=/private/tmp PYTHONPATH=plugins/lazarus uv run --no-project --python 3.14.6 --with-requirements plugins/lazarus/requirements.lock python plugins/lazarus/examples/goldfinch-v1/demo.py build-fixture --out /private/tmp/fiat881-step1-goldfinch-v1`, a byte-for-byte tree comparison, and the test command below.
**Files.** `plugins/lazarus/scripts/lazarus_lib/paths.py`; the manifest and verifier modules whose public helpers need descriptor-root overloads; `plugins/lazarus/examples/goldfinch-v1/demo.py`; `plugins/lazarus/tests/test_paths.py`; `plugins/lazarus/tests/test_goldfinch.py`; committed prose copies at `plugins/lazarus/docs/macos-path-repair-study.md` and `plugins/lazarus/docs/macos-path-repair-runbook.md`.
**Tests.** Extend descriptor-ownership, non-directory, stable-read, stage-rename, parent-swap, no-replace, rollback, cleanup, and exact-tree tests. Elenchus command: `python3 plugins/lazarus/tests/run_tests.py --elenchus-report {report}`. Report format: `unittest-json-v1`. Report file: `.hexaemeron/step-1-elenchus.json`.
**Disciplines.** phylax: caller-supplied output names and directory descriptors cross the filesystem boundary. ephoros: the structured runner must show Darwin execution, exact Python, counts, skips, exit, and fixture equality. metron: no speed claim; keep zero path materialisations and no new whole-tree copy. elenchus: both the pseudo-FD failure and existing race guards must be red or preserved on the base and green after repair. hypomnema: explain descriptor duplication and the refusal to materialise an inode as a mutable path beside the confinement helper.

## Step 2: Admit only checked Darwin root aliases

**Goal.** Allow a statement below an ordinary macOS temporary directory while preserving no-follow traversal for every user-controlled parent and the final file.
**Entry.** Step 1's receipted branch and green Lazarus report, with descriptor-root fixture behavior unchanged.
**Exit.** On Darwin only, the statement walk admits lexical root entries `var`, `tmp`, and `etc` solely when each exact link text, target directory, and target identity matches its `private/*` contract; it rechecks the alias before reading and then resumes the existing descriptor-relative no-follow walk. Linux keeps its current root walk. Prove the exit by copying the checked statement into Python's default `TemporaryDirectory`, calling `write_release`, running `verify_release`, and executing the test command below.
**Files.** `plugins/lazarus/scripts/lazarus_lib/release.py`; `plugins/lazarus/tests/test_release.py`; `plugins/lazarus/docs/preservation-release.md` only if the accepted alias surface needs an operator-facing note.
**Tests.** Add the default macOS temporary-path success case and refusals for a wrong root link target, changed target identity, nested alias, user-controlled symlink parent, symlink final file, and alias drift between checks. Preserve the existing stable-read and containment tests. Elenchus command: `python3 plugins/lazarus/tests/run_tests.py --elenchus-report {report}`. Report format: `unittest-json-v1`. Report file: `.hexaemeron/step-2-elenchus.json`.
**Disciplines.** phylax: one OS-owned root transition is an explicit exception inside an otherwise no-follow walk. ephoros: tests must record which fixed alias class was accepted without exposing temporary path suffixes or statement bytes. metron: none, because the step makes no latency or throughput claim. elenchus: the `/var` base failure must turn green while hostile non-root symlinks remain guarded. hypomnema: keep the exact three-name alias policy beside the helper and tests; any future addition requires a new trust-boundary decision.

## Step 3: Hold both regressions on Darwin and demonstrate the repair

**Goal.** Add durable macOS execution for the two repaired paths and demonstrate the complete issue acceptance surface on exact Python 3.14.6.
**Entry.** Step 2's receipted branch, green full Lazarus report, and unchanged checked fixture and release bytes.
**Exit.** The existing Lazarus workflow retains its Linux job and adds the smallest issue-authorised macOS job or equivalent durable Darwin runner. It installs exact Python 3.14.6, runs the complete Lazarus suite with both regressions unskipped, rebuilds the Goldfinch fixture to a fresh directory, compares every byte with the checked fixture, builds a release from the default macOS temporary statement path, and verifies that release. Prove the exit with `python3 plugins/lazarus/tests/test_scaffold.py`, the test command below, and one local Darwin execution of both demo paths.
**Files.** `.github/workflows/lazarus.yml`; `plugins/lazarus/tests/test_scaffold.py`; focused regression tests only if final runner assertions require them; the two committed issue study/runbook copies if their final command text must be synchronised.
**Tests.** Extend the workflow-shape assertion to require the pinned macOS job, both regression commands, complete suite, exact-tree comparison, and release verification without new permissions or network capture. Elenchus command: `python3 plugins/lazarus/tests/run_tests.py --elenchus-report {report}`. Report format: `unittest-json-v1`. Report file: `.hexaemeron/step-3-elenchus.json`.
**Disciplines.** phylax: CI gains no secret, write, or capture authority and runs only offline fixture/release operations. ephoros: one commit SHA must correlate OS, architecture, Python, test and skip counts, fixture comparison, and release verification. metron: none, because this is compatibility evidence rather than a performance change. elenchus: the final demonstration reruns both original failures plus all hostile-path guards and refuses an unexpected skip. hypomnema: the existing workflow is the durable home; no new workflow or ADR is warranted.

### Amendment -- 2026-08-31

**What changed.** Complete replacement Exit: Descriptor-root helpers duplicate caller-owned directory descriptors, preserve existing path-root callers, and keep component reads, manifest writes, verification, finalisation, rollback, and cleanup anchored to the pinned stage. The two checked Goldfinch `demo.py` copies use that interface. Their directly derived fixture manifests, release statement, and release binding are rebuilt in place under the existing version; every other preserved source component remains byte-identical, and no superseded artefact or version bump is retained. The Goldfinch builder executes on Darwin without the pseudo-FD skip and reproduces the rebuilt `plugins/lazarus/examples/goldfinch-v1` fixture byte for byte. Prove the exit with `TMPDIR=/private/tmp PYTHONPATH=plugins/lazarus uv run --no-project --python 3.14.6 --with-requirements plugins/lazarus/requirements.lock python plugins/lazarus/examples/goldfinch-v1/demo.py build-fixture --out /private/tmp/fiat881-step1-goldfinch-v1`, a byte-for-byte tree comparison, `PYTHONPATH=plugins/lazarus uv run --no-project --python 3.14.6 --with-requirements plugins/lazarus/requirements.lock python plugins/lazarus/examples/goldfinch-v1/demo.py verify-release --release plugins/lazarus/examples/goldfinch-v1-release`, and the test command below. Complete replacement Files: `plugins/lazarus/scripts/lazarus_lib/paths.py`; `plugins/lazarus/scripts/lazarus_lib/manifest.py`; `plugins/lazarus/scripts/lazarus_lib/verifier.py`; `plugins/lazarus/examples/goldfinch-v1/demo.py`; `plugins/lazarus/examples/goldfinch-v1/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/demo.py`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/statement.json`; `plugins/lazarus/examples/goldfinch-v1-release/release.json`; `plugins/lazarus/tests/test_paths.py`; `plugins/lazarus/tests/test_goldfinch.py`; committed prose copies at `plugins/lazarus/docs/macos-path-repair-study.md` and `plugins/lazarus/docs/macos-path-repair-runbook.md`.
**Why.** The builder source is itself a digest-bound fixture component, so retaining its old derived bindings would either preserve the failure or make the release dishonest. Rebuilding only its derived same-version bindings keeps the evidence sealed to the corrected bytes without broadening the capture.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry broken; exit holds.

### Amendment -- 2026-08-31

**What changed.** Complete replacement Entry: Step 2's receipted branch, a green full Lazarus report, and a checked same-version fixture and release whose regenerated bindings describe the corrected `demo.py` bytes while every other preserved source component remains byte-identical.
**Why.** Step 3 must test the release produced by the corrected builder rather than require the stale digest bindings that encoded the macOS failure.
**Steps touched.** Step 3.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.

### Amendment -- 2026-08-31

**What changed.** Complete replacement Files: `plugins/lazarus/scripts/lazarus_lib/paths.py`; `plugins/lazarus/scripts/lazarus_lib/manifest.py`; `plugins/lazarus/scripts/lazarus_lib/verifier.py`; `plugins/lazarus/examples/goldfinch-v1/demo.py`; `plugins/lazarus/examples/goldfinch-v1/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/demo.py`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/statement.json`; `plugins/lazarus/examples/goldfinch-v1-release/release.json`; `plugins/lazarus/tests/test_paths.py`; `plugins/lazarus/tests/test_goldfinch.py`; `plugins/lazarus/tests/test_preservation_release.py`; committed prose copies at `plugins/lazarus/docs/macos-path-repair-study.md` and `plugins/lazarus/docs/macos-path-repair-runbook.md`.
**Why.** The preservation-release test records the checked fixture, statement, and release digests and asserts equality between the canonical and release fixture trees. Its three derived expectations must move with the same-version binding rebuild or the test would demand the corrupt digest chain.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.

### Amendment -- 2026-08-31

**What changed.** Complete replacement Files: `plugins/lazarus/scripts/lazarus_lib/paths.py`; `plugins/lazarus/scripts/lazarus_lib/manifest.py`; `plugins/lazarus/scripts/lazarus_lib/verifier.py`; `plugins/lazarus/examples/goldfinch-v1/demo.py`; `plugins/lazarus/examples/goldfinch-v1/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/demo.py`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/statement.json`; `plugins/lazarus/examples/goldfinch-v1-release/release.json`; `plugins/lazarus/tests/test_paths.py`; `plugins/lazarus/tests/test_goldfinch.py`; `plugins/lazarus/tests/test_preservation_release.py`; `plugins/lazarus/docs/macos-path-repair-study.md`; `plugins/lazarus/docs/macos-path-repair-runbook.md`; `.agents/skills/promise-machine/runtime/plugins/lazarus/scripts/lazarus_lib/paths.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/scripts/lazarus_lib/manifest.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/scripts/lazarus_lib/verifier.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1/demo.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1/manifest.json`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/fixture/demo.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/fixture/manifest.json`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/statement.json`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/release.json`; `.agents/skills/promise-machine/runtime/MANIFEST.json`; `.horos/boundary.json`.
**Why.** The repository requires generated Promise Machine installation copies to remain equal to their canonical sources and requires the Horos boundary to describe every changed classified file. These paths are mechanical consequences of the bounded canonical change, not a broader product surface.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.

### Amendment -- 2026-08-31

**What changed.** Complete replacement Files: `plugins/lazarus/scripts/lazarus_lib/paths.py`; `plugins/lazarus/scripts/lazarus_lib/manifest.py`; `plugins/lazarus/scripts/lazarus_lib/verifier.py`; `plugins/lazarus/examples/goldfinch-v1/demo.py`; `plugins/lazarus/examples/goldfinch-v1/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/demo.py`; `plugins/lazarus/examples/goldfinch-v1-release/fixture/manifest.json`; `plugins/lazarus/examples/goldfinch-v1-release/statement.json`; `plugins/lazarus/examples/goldfinch-v1-release/release.json`; `plugins/lazarus/tests/test_paths.py`; `plugins/lazarus/tests/test_goldfinch.py`; `plugins/lazarus/tests/test_preservation_release.py`; `plugins/lazarus/docs/macos-path-repair-study.md`; `plugins/lazarus/docs/macos-path-repair-runbook.md`; `.agents/skills/promise-machine/runtime/plugins/lazarus/scripts/lazarus_lib/paths.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/scripts/lazarus_lib/manifest.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/scripts/lazarus_lib/verifier.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1/demo.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1/manifest.json`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/fixture/demo.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/fixture/manifest.json`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/statement.json`; `.agents/skills/promise-machine/runtime/plugins/lazarus/examples/goldfinch-v1-release/release.json`; `.agents/skills/promise-machine/runtime/plugins/lazarus/docs/macos-path-repair-study.md`; `.agents/skills/promise-machine/runtime/plugins/lazarus/docs/macos-path-repair-runbook.md`; `.agents/skills/promise-machine/runtime/.horos/boundary.json`; `.agents/skills/promise-machine/runtime/MANIFEST.json`; `.horos/boundary.json`.
**Why.** Dependency-closed portable synchronisation also copies the two committed decision documents and carries its own Horos boundary. Naming those three paths completes the mechanical generated inventory without expanding the accepted implementation.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.

### Amendment -- 2026-08-31

**What changed.** Complete replacement Files: `plugins/lazarus/scripts/lazarus_lib/release.py`; `plugins/lazarus/tests/test_release.py`; `plugins/lazarus/docs/macos-path-repair-runbook.md`; `.agents/skills/promise-machine/runtime/plugins/lazarus/scripts/lazarus_lib/release.py`; `.agents/skills/promise-machine/runtime/plugins/lazarus/docs/macos-path-repair-runbook.md`; `.agents/skills/promise-machine/runtime/MANIFEST.json`; `.agents/skills/promise-machine/runtime/.horos/boundary.json`; `.horos/boundary.json`.
**Why.** The repository requires the dependency-closed Promise Machine installation copy to stay equal to the canonical Lazarus source, the durable runbook copy to retain this accepted scope, and both Horos boundaries to describe changed classified files. These are mechanical consequences of the bounded Step 2 change, not a wider alias surface.
**Steps touched.** Step 2.
**Still holding.** Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.
