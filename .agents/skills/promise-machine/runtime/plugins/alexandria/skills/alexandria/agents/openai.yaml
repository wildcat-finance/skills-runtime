interface:
  display_name: "Alexandria"
  short_description: "Digest-bound lending-data releases with a resumable Ethereum USDC interval collector, unsigned evidence statements, reviewed credit views and address queries."
  default_prompt: "Use Alexandria to ingest or verify a lending-data release, emit its unsigned in-toto statement, derive its reviewed credit view, query a verified address index, build and check the bounded Compound v3 Phase 0 method release, or collect, reconcile and verify a resumable Ethereum USDC interval, without hiding coverage gaps."
