# Runbook: emit an Ariadne-ready Alexandria release statement

## Step 1: Emit and demonstrate the Alexandria release statement

**Goal.** Ship one offline Alexandria command that turns a verified release into a deterministic, schema-bound in-toto Statement v1 and demonstrates its exact partial-verification boundary through Ariadne.

**Entry.** Run branch `fiat/407-emit-an-ariadne-ready-release-statement` at `58b7dcd1004bf8e6b0cf517bbcc778789e2c43ff`, with study SHA-256 `4a554d02d2296dac4a33b6d9ddf064407287265818d0cf81d202c1f8b1665eaf` receipted; issue #407 remains an open unassigned `wish`; `alexandria-v0.2.0` retains frontier revision `usdc-interval-collector` and digest `d5afa30db4e5769dccded9f28be061dc623e119b328dbbcd87b729567b7eaeff`; Alexandria package version is `0.2.1`. The plugin already has its layout, standard-library toolchain, root CI coverage and Apache-2.0 licence, so this step extends that scaffold rather than replacing it.

**Exit.** `plugins/alexandria/scripts/alexandria.py statement <release> --output <statement>` first completes the existing offline release verification, then writes canonical UTF-8 JSON through a fresh sibling temporary regular file and atomic replacement. It refuses a target inside or aliased into the release and leaves no final file on failure. The Statement v1 subject set contains exactly one logical release subject plus one subject for every manifest component, with every `sha256:` Alexandria identity converted to a full lowercase in-toto digest value. Predicate type `https://ariadne.wildcat.finance/alexandria-release/v1` carries the release format and digest, component names, confined object paths, media types, byte counts and digests, every capture's declared scope, coverage status and counts, unsupported collections and gaps, a passed offline-verification claim bound to the logical release digest, and an explicit empty command list. A closed JSON Schema and fixture drift test bind that shape. Repeated emission is byte-identical; malformed or tampered releases, missing subjects, bad digest prefixes, output aliases, symlinks, partial writes and schema drift are refused by focused guards. Ariadne `inspect` accepts the emitted bytes and reports the predicate unregistered and signatures unchecked; Ariadne `verify` runs the core gates while gates 2 and 5 remain visibly unchecked. No DSSE envelope, cosign call, publisher identity, provider-completeness claim, consensus-finality claim or canonical-chain claim is emitted. The canonical and portable skill copies, runtime contract, plugin README, marketplace prose and release-statement document state the command and those limits. The accepted study and this runbook are committed under `plugins/alexandria/docs/`. Alexandria advances by generation to `alexandria-v0.3.0` with the existing frontier revision, frontier digest, status and Next Fiat job byte-identical; the delivery package advances to `0.3.0` across both plugin manifests, the Claude marketplace and `tests/test_version_propagation.py`. Prove the complete exit with the deterministic credit-history demo followed by `statement`, Ariadne `inspect` and Ariadne `verify`; `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`; `python3 -m unittest discover -s plugins/ariadne/tests -t plugins/ariadne`; `python3 -m unittest discover -s tests`; `python3 scripts/promise_machine.py check`; Protasis over both committed design records; Imprimatur and Brevitas over every changed prose file; `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`; `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests`; `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents plugins docs`; complete JSON parsing; canonical-to-portable skill equality; `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` after staging the intended tree; and `git diff --cached --check`.

**Files.** Create `plugins/alexandria/scripts/alexandria_lib/statement.py`, `plugins/alexandria/schemas/release-statement-v1.schema.json`, `plugins/alexandria/tests/test_statement.py`, `plugins/alexandria/tests/run_tests.py`, `plugins/alexandria/tests/test_runner.py`, `plugins/alexandria/docs/release-statements.md`, `plugins/alexandria/docs/release-statement-study.md`, and `plugins/alexandria/docs/release-statement-runbook.md`. Update `plugins/alexandria/scripts/alexandria.py`, `plugins/alexandria/scripts/alexandria_lib/__init__.py`, `plugins/alexandria/tests/test_scaffold.py`, `plugins/alexandria/AGENTS.md`, `plugins/alexandria/README.md`, `plugins/alexandria/skills/alexandria/SKILL.md`, `.agents/skills/alexandria/SKILL.md`, `plugins/alexandria/skills/alexandria/EVOLUTION.md`, `plugins/alexandria/.claude-plugin/plugin.json`, `plugins/alexandria/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `tests/test_version_propagation.py`. Update `.horos/boundary.json` and `.horos/candidates.json` only with the required complete staged-tree scan if their generated bytes change.

**Tests.** Add focused library and CLI cases for raw and derived releases, exact subjects, exact predicate projection, zero and non-zero counts, heterogeneous scopes, empty and non-empty gaps, unsupported collections, stable ordering and bytes, schema agreement, Ariadne inspect and core-gate output, tampered releases, hostile output paths, symlinks, existing targets, interrupted writes and controlled errors without tracebacks. Add runner guards for absent, duplicate, empty, outside-worktree, existing, symlinked and replaced report targets plus successful and failing result counters. The Elenchus runner contract is `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/alexandria-unittest.json`. The focused, Alexandria, Ariadne and root suites must exit zero; counts are reported from their current outputs rather than predicted.

**Disciplines.** phylax: the release and output path are untrusted filesystem input, so the step inherits bounded verification and adds no-follow confinement plus an atomic write. ephoros: this is a bounded local command, so stable stdout, controlled stderr, exit status and Ariadne's explicit unchecked report answer the study's operator questions without a daemon or telemetry service. metron: no performance claim is made; the study names the same-path timing command if one is proposed later. elenchus: every implementation or audit failure receives a minimal parent-failing guard, and the declared runner emits the source-bound report. hypomnema: the generation ledger row owns the governed-skill decision while the committed study, runbook, schema and release-statement document preserve the public wire and signing boundaries without a duplicate ADR.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Exit: `plugins/alexandria/scripts/alexandria.py statement <release> --output <statement>` first completes the existing offline release verification, then writes canonical UTF-8 JSON through a fresh sibling temporary regular file and atomic replacement. It refuses a target inside or aliased into the release and leaves no final file on failure. The Statement v1 subject set contains exactly one logical release subject plus one subject for every manifest component, with every `sha256:` Alexandria identity converted to a full lowercase in-toto digest value. Predicate type `https://ariadne.wildcat.finance/alexandria-release/v1` carries the release format and digest, component names, confined object paths, media types, byte counts and digests, every capture's declared scope, coverage status and counts, unsupported collections and gaps, a passed offline-verification claim bound to the logical release digest, and an explicit empty command list. A closed JSON Schema and fixture drift test bind that shape. Repeated emission is byte-identical; malformed or tampered releases, missing subjects, bad digest prefixes, output aliases, symlinks, partial writes and schema drift are refused by focused guards. Ariadne `inspect` accepts the emitted bytes and reports the predicate unregistered and signatures unchecked; Ariadne `verify` runs the core gates while gates 2 and 5 remain visibly unchecked. No DSSE envelope, cosign call, publisher identity, provider-completeness claim, consensus-finality claim or canonical-chain claim is emitted. The canonical skill, Promise Machine router, runtime contract, plugin README, marketplace prose and release-statement document state the command and those limits; the root portable-entry test continues to prove that `.agents/skills/promise-machine/SKILL.md` is the only portable entrypoint. The accepted study and this amended runbook are committed under `plugins/alexandria/docs/`. Alexandria advances by generation to `alexandria-v0.3.0` with the existing frontier revision, frontier digest, status and Next Fiat job byte-identical; the delivery package advances to `0.3.0` across both plugin manifests, the Claude marketplace and `tests/test_version_propagation.py`. Prove the complete exit with the deterministic credit-history demo followed by `statement`, Ariadne `inspect` and Ariadne `verify`; `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`; `python3 -m unittest discover -s plugins/ariadne/tests -t plugins/ariadne`; `python3 -m unittest discover -s tests`; `python3 scripts/promise_machine.py check`; Protasis over both committed design records; Imprimatur and Brevitas over every changed prose file; `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`; `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests`; `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents plugins docs`; complete JSON parsing; `python3 -m unittest tests.test_portable_skills.PortableSkillTests.test_promise_machine_is_the_only_portable_entrypoint`; `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` after staging the intended tree; and `git diff --cached --check`.

Complete replacement Files: Create `plugins/alexandria/scripts/alexandria_lib/statement.py`, `plugins/alexandria/schemas/release-statement-v1.schema.json`, `plugins/alexandria/tests/test_statement.py`, `plugins/alexandria/tests/run_tests.py`, `plugins/alexandria/tests/test_runner.py`, `plugins/alexandria/docs/release-statements.md`, `plugins/alexandria/docs/release-statement-study.md`, and `plugins/alexandria/docs/release-statement-runbook.md`. Update `plugins/alexandria/scripts/alexandria.py`, `plugins/alexandria/scripts/alexandria_lib/__init__.py`, `plugins/alexandria/tests/test_scaffold.py`, `plugins/alexandria/AGENTS.md`, `plugins/alexandria/README.md`, `plugins/alexandria/skills/alexandria/SKILL.md`, `plugins/alexandria/skills/alexandria/EVOLUTION.md`, `plugins/alexandria/.claude-plugin/plugin.json`, `plugins/alexandria/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `tests/test_version_propagation.py`. Update `.horos/boundary.json` and `.horos/candidates.json` only with the required complete staged-tree scan if their generated bytes change.

**Why.** Root `AGENTS.md` and `tests/test_portable_skills.py` make `.agents/skills/promise-machine/SKILL.md` the sole portable entrypoint. The baseline runbook carried a pre-router copy pattern and would have required Mason to violate current target law. The complete replacements remove that forbidden path and replace the stale equality check with the existing sole-entrypoint guard; the product capability, public predicate, evidence boundary, versions and tests are otherwise unchanged.

**Steps touched.** Step 1's Exit and Files fields.

**Still holding.** Step 1: entry holds; exit holds.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Files: Create `plugins/alexandria/scripts/alexandria_lib/statement.py`, `plugins/alexandria/schemas/release-statement-v1.schema.json`, `plugins/alexandria/tests/test_statement.py`, `plugins/alexandria/tests/run_tests.py`, `plugins/alexandria/tests/test_runner.py`, `plugins/alexandria/docs/release-statements.md`, `plugins/alexandria/docs/release-statement-study.md`, and `plugins/alexandria/docs/release-statement-runbook.md`. Update `plugins/alexandria/scripts/alexandria.py`, `plugins/alexandria/scripts/alexandria_lib/__init__.py`, `plugins/alexandria/tests/test_scaffold.py`, `plugins/alexandria/AGENTS.md`, `plugins/alexandria/README.md`, `plugins/alexandria/skills/alexandria/SKILL.md`, `plugins/alexandria/skills/alexandria/agents/openai.yaml`, `plugins/alexandria/skills/alexandria/EVOLUTION.md`, `plugins/alexandria/.claude-plugin/plugin.json`, `plugins/alexandria/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `tests/promise_machine_coverage.json`, `tests/test_promise_machine_contract.py`, and `tests/test_version_propagation.py`. Update `.horos/boundary.json` and `.horos/candidates.json` only with the required complete staged-tree scan if their generated bytes change.

**Why.** The new `alexandria-release-statement` transition made three root contract guards fail: the OpenAI host description retained the old short description, and the Promise Machine frozen coverage and structure registries did not yet name the new executable promise. Those guards are part of the receipted Exit and cannot pass without updating their governed inputs. Adding the exact three paths makes the implementation boundary honest; no product behavior or held frontier changes.

**Steps touched.** Step 1's Files field.

**Still holding.** Step 1: entry holds; exit holds.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Files: Create `plugins/alexandria/scripts/alexandria_lib/statement.py`, `plugins/alexandria/schemas/release-statement-v1.schema.json`, `plugins/alexandria/tests/test_statement.py`, `plugins/alexandria/tests/run_tests.py`, `plugins/alexandria/tests/test_runner.py`, `plugins/alexandria/docs/release-statements.md`, `plugins/alexandria/docs/release-statement-study.md`, and `plugins/alexandria/docs/release-statement-runbook.md`. Update `plugins/alexandria/scripts/alexandria.py`, `plugins/alexandria/scripts/alexandria_lib/__init__.py`, `plugins/alexandria/tests/test_scaffold.py`, `plugins/alexandria/AGENTS.md`, `plugins/alexandria/README.md`, `plugins/alexandria/schemas/README.md`, `plugins/alexandria/skills/alexandria/SKILL.md`, `plugins/alexandria/skills/alexandria/agents/openai.yaml`, `plugins/alexandria/skills/alexandria/EVOLUTION.md`, `plugins/alexandria/.claude-plugin/plugin.json`, `plugins/alexandria/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `tests/promise_machine_coverage.json`, `tests/test_promise_machine_contract.py`, and `tests/test_version_propagation.py`. Update `.horos/boundary.json` and `.horos/candidates.json` only with the required complete staged-tree scan if their generated bytes change.

**Why.** The implementation correctly indexed the new public schema in Alexandria's schema catalogue, but the second Files replacement omitted that existing document. Keeping the catalogue stale would make the schema harder to discover; keeping the changed path outside the source-bound step would make the implementation record false. This replacement adds only that path.

**Steps touched.** Step 1's Files field.

**Still holding.** Step 1: entry holds; exit holds.
