# Imprimatur source-prose extraction runbook

## Step 1: Extract source prose and guard its boundaries

**Goal.** Add offset-preserving prose extraction for `.sol`, `.py`, `.ts`, and `.tsx`, guard the reproduced false clean and adjacent lexical cases, and record the Imprimatur generation without moving its held frontier.

**Entry.** Start from run branch `fiat/503-imprimatur-1-read-comment-spans-in-source-fi` at `0f835d5f5f7c95ad2716eb63bd9bdd8f68b0a841`, with the study receipted and the existing Imprimatur, Hexaemeron, and root suites green.

**Exit.** `imprimatur.py` dispatches supported source paths to deterministic adapters, retains only comments and Python docstrings in a same-length mask, reports original coordinates, refuses named extraction failures, and keeps Markdown and standard-input behavior unchanged. The existing repository layout, Python toolchain, CI entrypoints, and Apache-2.0 licence remain the green scaffold; no parallel scaffold is added. The Imprimatur skill is `imprimatur-v2.3.0` on the generation axis with frontier revision, digest, status, and held job unchanged. The accepted study and runbook are committed beside the skill. Prove the exit with `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 -m unittest tests.test_evolution_contract tests.test_version_propagation`, and an end-to-end issue specimen passed to `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py <fixture> --max-defects 0`, which must exit 1 and report the fixture's original line and column.

**Files.** Change `plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py`, `plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `plugins/hexaemeron/skills/imprimatur/SKILL.md`, and `plugins/hexaemeron/skills/imprimatur/EVOLUTION.md`. Add `plugins/hexaemeron/tests/test_imprimatur_source_extraction.py`, `plugins/hexaemeron/docs/imprimatur-source-prose-extraction/study.md`, and `plugins/hexaemeron/docs/imprimatur-source-prose-extraction/runbook.md`. Verify the existing `.github` checks, test entrypoints, and `LICENSE` remain the project scaffold rather than duplicating them.

**Tests.** Add source-language positive cases, literal and URL negatives, Python docstring ownership cases, same-length and coordinate assertions, malformed-input refusal, Markdown regression cases, and multi-file CLI cases. The Elenchus runner contract is: test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `.hexaemeron/elenchus/step-1.json`. Run the focused Imprimatur suite, the complete Hexaemeron suite, and the evolution and version-propagation tests named in Exit. Expected counts are whatever the clean entry reports plus the committed new cases; record the exact fixed-tree counts rather than predicting them.

**Disciplines.** phylax: supported local source is untrusted parser input, so suffix selection is closed, inspected code is never imported or executed, and lexical failures refuse a clean result. ephoros: none, this is a synchronous local lint with immediate structured output and no unattended path. metron: none, the step makes no performance claim or budget change. elenchus: issue 503 is a reproduced failure, so each causal boundary receives a guard seen red on the unfixed parent and green on the fixed tree. hypomnema: the adapter choice and rejected alternatives belong in Imprimatur's generation row, while the public source-mode contract belongs in its `SKILL.md`.

## Step 2: Propagate the package and demonstrate the delivered command

**Goal.** Make the changed Hexaemeron package discoverable at one consistent package version, run every affected repository gate, and preserve a reproducible proof of the source-prose behavior.

**Entry.** Start from the controller-recorded Step 1 commit with its focused, Hexaemeron, evolution, and version-propagation exits green and no unreceipted change to the accepted study or runbook.

**Exit.** The smallest repository-valid Hexaemeron package increment is identical in both plugin manifests, both marketplace listings, and `tests/test_version_propagation.py`. A proof document records the issue specimen and the exact command, exit, source coordinates, source-language matrix, malformed-input refusal, version identities, and complete gate results. Prove the exit with `python3 scripts/promise_machine.py check`, `python3 -m unittest discover -s tests`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents plugins docs`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py plugins/hexaemeron/docs/imprimatur-source-prose-extraction/study.md plugins/hexaemeron/docs/imprimatur-source-prose-extraction/runbook.md plugins/hexaemeron/docs/imprimatur-source-prose-extraction/proof.md --max-defects 0`, and `git diff --check`.

**Files.** Change `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, and `tests/test_version_propagation.py`. Add `plugins/hexaemeron/docs/imprimatur-source-prose-extraction/proof.md`. Regenerate `.horos/boundary.json` only if the required Horos scan reports that its classified tracked-tree evidence changed.

**Tests.** Re-run every command in Exit after the proof carries final counts. Exercise the CLI against temporary `.sol`, `.py`, `.ts`, and `.tsx` fixtures and one ordinary Markdown fixture, recording paths and results without committing the temporary inputs. The Elenchus runner contract is: test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `.hexaemeron/elenchus/step-2.json`. Run `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` only when classified-file drift must be repaired, then rerun the root and Horos checks.

**Disciplines.** phylax: package and proof inputs are repository-owned and introduce no new host, dependency, subprocess wrapper, credential, or model-output authority; the existing linter still checks the changed Python paths. ephoros: none, packaging and a local proof add no unattended execution or retained telemetry. metron: none, the demonstration records correctness and no timing claim. elenchus: any failed full gate stops publication and is reduced to its causal change before the same gate set is rerun; the source-bound report command remains fixed above. hypomnema: the proof preserves what was demonstrated and the package manifests remain the established homes for distribution identity, with no second decision record.
