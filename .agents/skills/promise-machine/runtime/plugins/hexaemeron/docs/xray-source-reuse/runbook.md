# Runbook: reuse source-bound X-Ray analysis across Fiat audit rounds

## Step 1: Build the source-bound reuse adapter

**Goal.** Add the first-party planning, entry-validation, assembly, promotion, fixture, and durable design surfaces without changing the vendored X-Ray tree.

**Entry.** Run branch `fiat/510-reuse-source-bound-x-ray-analysis-across-fia` at `5489863196006d8e8b45799d74b56208cac65e4d`, with study SHA-256 `00e27b3b39f9f3c01fa738b91e172cac95a34bd6fbcf48ca619d8fb3dba6cae6` receipted and the current X-Ray `SKILL.md` SHA-256 equal to `b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41`.

**Exit.** `plugins/hexaemeron/lib/xray_reuse.py` validates bounded scope and fact JSON, plans full or incremental extraction from byte and dependency drift, assembles the exact current union, rebuilds complete write-site inputs, and promotes a candidate only after all four output digests are present. Fixtures cover unchanged reuse, body-only drift, transitive dependency drift, write-site drift, source removal, corrupt-cache full fallback, unsafe paths, partial candidates, and interrupted promotion. The accepted study and runbook are committed beside a new ADR-036. Prove the exit with `python3 -m unittest plugins.hexaemeron.tests.test_xray_reuse`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 -m unittest discover -s tests`, the Protasis checks over both committed documents, the Imprimatur and Brevitas repository lints over changed prose, `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins/hexaemeron/lib/xray_reuse.py plugins/hexaemeron/tests/test_xray_reuse.py`, `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins/hexaemeron/lib/xray_reuse.py plugins/hexaemeron/tests/test_xray_reuse.py`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py docs/decisions/ADR-036-keep-x-ray-reuse-outside-vendored-source-and-fiat-state.md plugins/hexaemeron/docs/xray-source-reuse`, `shasum -a 256 plugins/hexaemeron/skills/x-ray/SKILL.md`, and `git diff --check`.

**Files.** Create `plugins/hexaemeron/lib/xray_reuse.py`, `plugins/hexaemeron/tests/test_xray_reuse.py`, `plugins/hexaemeron/tests/fixtures/xray-reuse/`, `plugins/hexaemeron/docs/xray-source-reuse/study.md`, `plugins/hexaemeron/docs/xray-source-reuse/runbook.md`, and `docs/decisions/ADR-036-keep-x-ray-reuse-outside-vendored-source-and-fiat-state.md`.

**Tests.** Add the focused unittest module and hostile fixtures named in Exit. The Elenchus runner contract is `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/hexaemeron-unittest.json`. Focused, Hexaemeron, and root suites must execute with zero failures; counts are reported from their current outputs rather than predicted.

**Disciplines.** phylax: untrusted JSON, model-produced entries, source paths, and candidate filesystem writes receive closed schemas, caps, containment, and atomic replacement. ephoros: no daemon or scheduled service is added; stable JSON results answer the operator questions named in the study. metron: no performance claim is made in this step; measurement belongs to step 3. elenchus: every surfaced defect gets a hostile fixture and parent-failing guard before the suites rerun. hypomnema: ADR-036 records the cross-cutting ownership and storage decision, while the accepted study and runbook become the implementation record.

## Step 2: Bind reuse to the X-Ray audit operation

**Goal.** Make the first-party adapter part of Hexaemeron's digest-pinned X-Ray composition without adding cache material or authority to Fiat state.

**Entry.** Step 1's signed branch and green exit, with the adapter and its protocol committed and the vendored X-Ray digest unchanged.

**Exit.** `plugins/hexaemeron/PROMISES.md` declares a bounded source-reuse overlay tied to the current vendored X-Ray digest and adapter evidence. `xray-reuse.md`, `audit-loop.md`, and Warden's packet instructions require full logical scope, fresh global synthesis, four regenerated outputs, and named full recomputation on cache uncertainty. Tests establish that the adapter is invoked only as an X-Ray preparation layer and that `hexctl` state, ledger, audit directives, and receipts gain no cache key or payload. Prove the exit with the focused X-Ray-reuse tests, `python3 scripts/promise_machine.py check --only contracts,overlays`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 -m unittest discover -s tests`, Imprimatur and Brevitas over changed prose, Phylax, Ephoros, and Hypomnema over the changed first-party paths, the unchanged X-Ray SHA-256 check, and `git diff --check`.

**Files.** Create `plugins/hexaemeron/skills/fiat/references/xray-reuse.md`; update `plugins/hexaemeron/PROMISES.md`, `plugins/hexaemeron/skills/fiat/references/audit-loop.md`, `plugins/hexaemeron/agents/warden.md`, `plugins/hexaemeron/tests/test_xray_reuse.py`, and the smallest existing Promise Machine or Fiat instruction tests required by the changed contract.

**Tests.** Extend focused tests with exact overlay, Warden, audit-loop, full-scope, four-output, and no-Fiat-cache assertions. The Elenchus runner contract is `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/hexaemeron-unittest.json`. Every named check must exit zero; the Promise Machine check remains a structural result and does not attest analysis correctness.

**Disciplines.** phylax: the integration passes only validated adapter outputs to agent instructions and never turns model text into a command or path. ephoros: JSON operation results stay the only signals because no unattended runtime is introduced. metron: none, this step changes composition and evidence boundaries rather than speed. elenchus: instruction and receipt-shape regressions receive focused assertions before the full suites. hypomnema: the overlay is the durable evidence contract and `xray-reuse.md` is the operator home; neither duplicates ADR-036's rejected alternatives.

## Step 3: Demonstrate reuse and publish the package surfaces

**Goal.** Record the bounded before-and-after evidence, propagate the Hexaemeron package revision, and prove the complete current-tree contribution path.

**Entry.** Step 2's signed branch and green exit, with adapter, overlay, and Fiat/Warden integration committed.

**Exit.** The multi-contract fixture runs the full, unchanged, body-only, dependency, write-site, removal, and corrupt-cache cases; the proof records extraction counts, three same-command wall-time samples and spread, final-output digests, environment, and limits. The unchanged run has zero fresh extractions, reuses every fixture entry, and produces byte-identical four-output digests; drift cases dirty the expected closure and never reuse a removed or stale row. Hexaemeron package version `1.6.3` agrees across both plugin manifests, both marketplace records, and `tests/test_version_propagation.py`. The vendored X-Ray digest remains exact. Prove the exit with the recorded fixture command, focused tests, complete Hexaemeron and root suites, `python3 scripts/promise_machine.py check`, all repository-required lints over changed paths, `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` followed by its currency test when classified files changed, manifest JSON parsing, X-Ray tree comparison to `main`, and `git diff --check`.

**Files.** Create `plugins/hexaemeron/docs/xray-source-reuse/proof.md`; update the fixture runner and `plugins/hexaemeron/tests/test_xray_reuse.py` only as the measured demonstration requires; update `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, and `tests/test_version_propagation.py`; update `.horos/boundary.json` only if the required scan changes it.

**Tests.** Preserve raw fixture measurements in the proof and require all correctness outputs before keeping the reuse path. The Elenchus runner contract is `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/hexaemeron-unittest.json`. Focused, Hexaemeron, Promise Machine, and root checks must all exit zero from the final tree; package surfaces must all report `1.6.3`.

**Disciplines.** phylax: the demonstration uses only checked local fixtures and writes bounded proof artefacts, with no network, credential, or new dependency. ephoros: none, the proof is a one-shot command and no alerting surface exists. metron: this step runs the same fixture command three times before and after, records counts, timings, spread, and correctness digests, and limits the conclusion to that fixture. elenchus: any red fixture or suite stops publication and is worked to a parent-failing guard. hypomnema: `proof.md` is the durable measurement record; package versions live only in their established manifests and propagation assertion.


### Amendment -- 2026-08-25

**What changed.** Complete replacement Exit: `plugins/hexaemeron/lib/xray_reuse.py` validates bounded scope and fact JSON, plans full or incremental extraction from byte and dependency drift, assembles the exact current union, rebuilds complete write-site inputs, and promotes a candidate only after all four output digests are present. Fixtures cover unchanged reuse, body-only drift, transitive dependency drift, write-site drift, source removal, corrupt-cache full fallback, unsafe paths, partial candidates, and interrupted promotion. The accepted study and amended runbook are committed beside a new ADR-036. Stage the complete intended file set before the Horos write so its tracked-tree universe includes every new path; stage both generated Horos records after the write. Prove the exit with `python3 -m unittest plugins.hexaemeron.tests.test_xray_reuse`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 -m unittest discover -s tests`, the Protasis checks over both committed documents, the Imprimatur and Brevitas repository lints over changed prose, `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins/hexaemeron/lib/xray_reuse.py plugins/hexaemeron/tests/test_xray_reuse.py`, `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins/hexaemeron/lib/xray_reuse.py plugins/hexaemeron/tests/test_xray_reuse.py`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py docs/decisions/ADR-036-keep-x-ray-reuse-outside-vendored-source-and-fiat-state.md plugins/hexaemeron/docs/xray-source-reuse`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `shasum -a 256 plugins/hexaemeron/skills/x-ray/SKILL.md`, and `git diff --cached --check`.

Complete replacement Files: Create `plugins/hexaemeron/lib/xray_reuse.py`, `plugins/hexaemeron/tests/test_xray_reuse.py`, `plugins/hexaemeron/tests/fixtures/xray-reuse/`, `plugins/hexaemeron/docs/xray-source-reuse/study.md`, `plugins/hexaemeron/docs/xray-source-reuse/runbook.md`, and `docs/decisions/ADR-036-keep-x-ray-reuse-outside-vendored-source-and-fiat-state.md`; update `.horos/boundary.json` and `.horos/candidates.json` with the required staged-tree scan.

**Why.** `git diff --check` did not inspect the untracked implementation paths, while the first staged check exposed the receipted runbook's terminal blank line. The repository also requires Horos to derive its boundary from the complete tracked tree. Appending this checked replacement preserves the receipted prefix, gives the committed runbook a nonblank terminal block, and makes both gates explicit.

**Steps touched.** Step 1's Exit and Files fields.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Exit: `plugins/hexaemeron/lib/xray_reuse.py` carries a closed, capped per-source preparation schema sufficient for every source-bound input required by the digest-pinned X-Ray operation: declarations, types, inheritance and imports; roles and access; state, value and fund-flow facts; entry points, calls and key logic; guards, symbolic deltas and transitions; invariant inputs; and write sites. Before planning, the wrapper preserves X-Ray's complete current source boundary and performs every required source read and digest. Reused entries replace only regeneration of validated preparation facts; coverage, history, integration, cross-source and global synthesis remain fresh. The exact current union feeds fresh global synthesis and regenerates `architecture.json`, `x-ray.md`, `entry-points.md`, and `invariants.md`; any unsafe or incomplete current scope stops the run, and any cache uncertainty produces named full recomputation. The Promise Machine overlay, `xray-reuse.md`, `audit-loop.md`, and Warden packet bind this handoff to the unchanged vendored X-Ray digest without adding cache authority to Fiat. Prove the exit with `python3 -m unittest plugins.hexaemeron.tests.test_xray_reuse`, `python3 scripts/promise_machine.py check --only contracts,overlays`, `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, `python3 -m unittest discover -s tests`, Imprimatur and Brevitas over changed prose, Phylax, Ephoros, and Hypomnema over the changed first-party paths, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, exact SHA-256 checks for `plugins/hexaemeron/skills/x-ray/SKILL.md` and `plugins/hexaemeron/lib/xray_reuse.py`, and `git diff --cached --check`.

Complete replacement Files: Create `plugins/hexaemeron/skills/fiat/references/xray-reuse.md`; update `plugins/hexaemeron/lib/xray_reuse.py`, `plugins/hexaemeron/PROMISES.md`, `plugins/hexaemeron/skills/fiat/references/audit-loop.md`, `plugins/hexaemeron/agents/warden.md`, `plugins/hexaemeron/tests/test_xray_reuse.py`, `plugins/hexaemeron/tests/test_fiat_skill.py`, `plugins/hexaemeron/tests/test_hexctl.py`, the smallest fixture files under `plugins/hexaemeron/tests/fixtures/xray-reuse/` needed to prove complete preparation entries and equivalent four-output synthesis, and `plugins/hexaemeron/docs/xray-source-reuse/runbook.md`; update `.horos/boundary.json` and `.horos/candidates.json` only through the required complete staged-tree scan.

Complete replacement Tests: Extend the focused suite with a parent-red compatibility guard proving that every pinned X-Ray source read remains mandatory, the closed preparation schema covers every reusable source-bound input, a full run and an unchanged reuse run produce equivalent four-output bytes, fresh global synthesis consumes the exact current union, uncertain or incomplete facts force refusal or named full recomputation, and no X-Ray working material enters Fiat state, ledger, directive or receipt authority. The Elenchus runner contract is `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/hexaemeron-unittest.json`. Focused, Hexaemeron, Promise Machine and root checks must exit zero from the final tree; the Promise Machine result remains structural and does not attest analysis correctness.

Complete replacement Disciplines: phylax: expanded model-produced preparation facts remain closed, bounded data tied to current source and dependency digests, never commands or unchecked paths. ephoros: bounded one-shot JSON results remain the only operation signals; no daemon, metric, trace or alert is added. metron: no speed claim belongs to this step; Step 3 measures only fixture extraction counts and equivalent outputs. elenchus: the operation-compatibility contradiction and every later defect receive parent-failing guards before final-tree suites. hypomnema: the amended runbook and `xray-reuse.md` state the full-read, reusable-preparation and fresh-synthesis boundary without changing ADR-036's ownership decision.

**Why.** Step 2 audit finding `S2-R2-01` proved that the first binding told X-Ray to extract only dirty sources even though the pinned X-Ray operation requires every scoped source to be read, and the initial preparation schema omitted source-bound facts that the four current outputs require. Leaving that contract in place would either repeat all extraction while claiming reuse or violate the digest-pinned operation. The bounded expansion preserves every X-Ray read, makes the reusable unit complete enough for its stated role, keeps global conclusions fresh, and leaves vendored X-Ray and Fiat state unchanged.

**Steps touched.** Step 2's Exit, Files, Tests, and Disciplines fields.

**Still holding.** Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.
