interface:
  display_name: "Phylax"
  short_description: "Guards the off-chain surface at its boundaries"
  default_prompt: "Use $hexaemeron:phylax to harden input, subprocess, secret, dependency, and job-scoped model proxy boundaries."
