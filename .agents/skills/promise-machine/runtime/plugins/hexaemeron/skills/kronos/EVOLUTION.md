# Kronos evolution ledger

Policy: [../VERSIONING.md](../VERSIONING.md)

- Current version: `kronos-v0.7.0`
- Frontier status: `mature`
- Frontier revision: `terminal-goal-loop`
- Current frontier: Kronos ranks eligible held Next Fiat jobs, selects the highest-value one, sets one durable goal, runs Fiat, and repeats until none remain.
- Next Fiat job: `None -- mature`

## History

| Version | Axis | Frontier revision | Frontier SHA-256 | Evidence | Change |
| --- | --- | --- | --- | --- | --- |
| `kronos-v0.0.0` | baseline | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | [skills#74](https://github.com/wildcat-finance/skills/issues/74) | Kronos starts here. It is complete and terminal by design. |
| `kronos-v0.1.0` | generation | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | Maintainer report: nine governed plugin ledgers were invisible to discovery | Scope now spans every plugin in the checkout rather than the invoking plugin alone, discovery descends into each plugin's own skills directory and names a skill by its own directory, ungoverned skills are reported instead of dropped, and the end-of-loop rescan re-evaluates every governed skill. Terminal by design is unchanged: the evolution counter stays at 0. |
| `kronos-v0.2.0` | generation | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | Maintainer request: constrain a native Kronos mode to the phase skills | Adds a fail-closed phase-only mode whose market is fixed to the six Hexaemeron phase skills, with an optional iteration cap. Ranking, Fiat ownership, repeated rescans and the full-market terminal loop are unchanged. |
| `kronos-v0.3.0` | generation | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | Wishlist kronos-1, taken at the maintainer's direction | Each ranking pass is now recorded. Step 4 appends the pass to `.kronos/scoreboard.jsonl` at the scope root and step 8 reads it back before reranking, so an axis score that moves for a held job nobody touched is visible rather than lost with the chat that carried it. A new `scripts/kronos.py` computes each candidate's held-job hash from that ledger on disk, as the SHA-256 of the canonical frontier line `VERSIONING.md` defines, so a recorded line can be checked against the digest the ledger already stores; taking a hash from the caller would have been a second way of naming one thing. The scoreboard is gitignored on purpose. Fiat refuses to start against a dirty tree, so a committed scoreboard would stop the loop's next iteration before it began, and having Kronos commit it instead would break the first hard rule. The cost is that the record lives on the machine that ran the loop. Ranking, the four axes and their caps, the tie-break and Fiat ownership are unchanged, and the writer records a judgement rather than making one. Terminal by design is unchanged: the evolution counter stays at 0 and the frontier is retained byte for byte. |
| `kronos-v0.4.0` | generation | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | Wishlist kronos-2, taken at the maintainer's direction | A blocked job can now be set down instead of stopping the frontier. The loop had two answers to a Fiat halt it could not clear, preserve and report or skip and be dishonest, so one job waiting on an approval nobody would grant held up every other eligible skill. `park` records the halt reason byte for byte beside the skill's held-job hash, selection skips parked candidates without dropping them from the ranking or their scores, and `parked` exits 3 while any stands, which is what stops step 8 declaring the loop complete. Parks live in `.kronos/parked.jsonl` rather than in the scoreboard, because the scoreboard is history where each line is what was true at that pass while a park is current state that changes, and reading one as the other costs a line its meaning. Staleness is decided by the held-job identity hash rather than by memory, so a park whose job has moved on says so; reversing that would leave every recorded park undated. A standing park blocks completion rather than warning about it, because a warning is a note nobody has to act on. Nothing unparks itself and an unreadable ledger counts as unknown, since an unreadable file is not evidence a blocker cleared. The four axes, their caps, the tie-break among unparked candidates and Fiat ownership are unchanged. Terminal by design is unchanged: the evolution counter stays at 0 and the frontier is retained byte for byte. |
| `kronos-v0.5.0` | generation | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | Wishlist kronos-3, taken at the maintainer's direction | The ranking can now be asked for on its own. Steps 1 to 4 produce the most decision-useful artefact here, a scored frontier across every governed skill, and there was no way to get it without committing to the week of Fiat that follows; the description sent anyone who wanted only the table away. Rank-only mode runs steps 1 to 4 and stops, and the description now admits it. Two fields join the recorded pass. `rank_only` says the ranking was looked at rather than acted on, which a missing run link cannot say on its own because `record` already accepted an absent run, so a delivery whose link was never filled in looked identical; a rank-only pass naming a run is refused. `ungoverned` carries step 2's report of in-scope skills with no ledger, which until now existed only in the chat that scrolled away, and a skill named there and scored in the same pass is refused because a scored candidate's held-job hash is computed from the ledger the report says it lacks. Rank-only is a field beside `mode` rather than a third value of it: `mode` says how wide the scope was and `rank_only` says whether the loop continued, so a rank-only pass over the six phase skills can record as both; folding them together would have lost one. Rank-only passes share the scoreboard with passes that launched deliveries, marked rather than separated, which keeps the drift check over one file. The four axes, their caps, the tie-break, the parked lane and Fiat ownership are unchanged. Terminal by design is unchanged: the evolution counter stays at 0 and the frontier is retained byte for byte. |
| `kronos-v0.6.0` | generation | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | [skills#462](https://github.com/wildcat-finance/skills/issues/462) | The scoreboard and parked lane now survive a recycled runner. `pull` copies the two JSONL files from `refs/heads/kronos/state` through a throwaway clone, and `push` fast-forwards that ref from the working copy. A missing ref is an empty start. An existing ref that cannot be read refuses rather than presenting an empty lane. A non-fast-forward push refuses and leaves local files. `record`, `park`, `unpark`, `show` and `parked` still start no subprocess. The four axes, their caps, the tie-break, rank-only mode and Fiat ownership are unchanged. Terminal by design is unchanged: the evolution counter stays at 0 and the frontier is retained byte for byte. |
| `kronos-v0.7.0` | generation | `terminal-goal-loop` | `ac28d95d80724aa001a92740f76416164e65d7b7b9cb5da43674d1ea73a214d1` | Maintainer direction, 2026-08-24, with the Fiat controller-currency delivery: [study](../../../../docs/fiat-controller-currency-study.md), [runbook](../../../../docs/fiat-controller-currency-runbook.md) | Step 8 now restores controller currency at the rescan boundary: run `hexctl currency`, and while it exits 3, reinstall every plugin it reports behind through this host's own installer, refresh, and re-resolve the paths before the next ranking, with the host mechanism cited from Fiat's plugin-currency reference rather than restated. A chain that merges its own pull requests otherwise drives every later run with the pins the chain started on, which is what the 2026-08-24 measurement caught: fourteen pins 10 commits behind within an hour. Terminal goal loop unchanged and still mature. |
