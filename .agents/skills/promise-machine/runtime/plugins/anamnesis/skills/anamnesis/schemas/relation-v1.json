{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wildcat.finance/anamnesis/schemas/relation-v1.json",
  "title": "anamnesis-relation/v1",
  "description": "One directed edge between two assertions. Edges are many-to-many in both directions: one change may answer several findings and one finding may need several changes. Collapsing either direction is a defect, not a simplification.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "kind", "from", "to", "policy_version"],
  "properties": {
    "id": {"type": "string", "minLength": 3, "maxLength": 300},
    "kind": {
      "enum": ["reported-as", "duplicate-of", "occurs-at", "addressed-by", "verified-by", "regressed-into", "supports-pattern"]
    },
    "from": {"type": "string"},
    "to": {"type": "string"},
    "policy_version": {"description": "The curation policy version that joined these two. A later policy may join them differently; the edge records which one did.", "type": "string"},
    "rationale": {"type": "string"}
  }
}
