- `reserve` at `Sound.sol:266` can now record/earmark more than the assets held. The old bound was `min(claims, held)`; the new bound is only `claims - unpaidTotal()`. In an illiquid state such as `claims = 100`, `held = 0`, and `unpaidTotal() = 0`, `reserve(100)` is accepted rather than clipped to `0`. This contradicts the adjacent fee rationale that an earmark “cannot exceed what is held” and can create an unsupported reservation. Smallest fix: retain the queue headroom while also applying the held bound, e.g. `min(held, claims - unpaidTotal())`. Add a regression test for that exact state and assert the recorded reservation remains `0`.

`accrueFee` at `Sound.sol:238` correctly changes the fee headroom from `claims - reserved` to `claims - unpaidTotal()`: using only the earmark can leave fee capacity equal to an illiquidity shortfall and reduce `claims` below the open batches’ total entitlement. A regression test should set `claims = 100`, `unpaidTotal() = 80`, and `reserved = 40`; `accrueFee(60)` must charge no more than `20`, leaving `claims >= 80`.

From this excerpt alone, it cannot be established whether `unpaidTotal()` includes every open batch exactly once, whether `reserve` mutates an earmark or queue total, or whether other invariants later reject an over-held reservation.